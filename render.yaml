services:
  - type: web
    name: stock-valuation-workbench
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: HOST=0.0.0.0 python app_server.py
    autoDeploy: true
